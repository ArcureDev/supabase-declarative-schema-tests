{
  "version": 1,
  "description": "Explicit PostgreSQL boundaries catalogue scenarios",
  "comment": "Table-driven pack for Explicit PostgreSQL boundaries. Shared project template; each scenario overlays one declarative file.",
  "firstCaseNumber": 590,
  "scenarios": [
    {
      "id": "boundary-access-method",
      "caseNumber": 590,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.access-method",
      "comment": "Covers PG-CAT-BND-01::boundary.access-method. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-01::boundary.access-method"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-large-object",
      "caseNumber": 591,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.large-object",
      "comment": "Covers PG-CAT-BND-01::boundary.large-object. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-01::boundary.large-object"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-database-settings",
      "caseNumber": 592,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.database-settings",
      "comment": "Covers PG-CAT-BND-01::boundary.database-settings. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-01::boundary.database-settings"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-database-create",
      "caseNumber": 593,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.database-create",
      "comment": "Covers PG-CAT-BND-02::boundary.database-create. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-02::boundary.database-create"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-tablespace-create",
      "caseNumber": 594,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.tablespace-create",
      "comment": "Covers PG-CAT-BND-02::boundary.tablespace-create. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-02::boundary.tablespace-create"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-temp-objects",
      "caseNumber": 595,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.temp-objects",
      "comment": "Covers PG-CAT-BND-03::boundary.temp-objects. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-03::boundary.temp-objects"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-sequence-value",
      "caseNumber": 596,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.sequence-value",
      "comment": "Covers PG-CAT-BND-03::boundary.sequence-value. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-03::boundary.sequence-value"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-prepared-xact",
      "caseNumber": 597,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.prepared-xact",
      "comment": "Covers PG-CAT-BND-03::boundary.prepared-xact. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-03::boundary.prepared-xact"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-cluster-role",
      "caseNumber": 598,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.cluster-role",
      "comment": "Covers PG-CAT-BND-04::boundary.cluster-role. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-04::boundary.cluster-role"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-superuser",
      "caseNumber": 599,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.superuser",
      "comment": "Covers PG-CAT-BND-04::boundary.superuser. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-04::boundary.superuser"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    },
    {
      "id": "boundary-stable-diagnostic",
      "caseNumber": 600,
      "expectation": "expected-unsupported",
      "description": "emit a stable diagnostic for boundary.stable-diagnostic",
      "comment": "Covers PG-CAT-BND-05::boundary.stable-diagnostic. Keep public.transition_anchor identity stable. This atom is an explicit supported/unsupported boundary, not an accidental omission.",
      "catalogueAtoms": [
        "PG-CAT-BND-05::boundary.stable-diagnostic"
      ],
      "requiredDiagnosticPatterns": [
        {
          "description": "reports a stable capability or scope diagnostic",
          "pattern": "\\b(?:code=unmodeled_kind|does\\s+not\\s+manage|scope\\s+database|unsupported|cannot)\\b"
        }
      ],
      "forbiddenDiagnosticPatterns": [
        {
          "description": "does not replace the transition anchor",
          "pattern": "\\bdrop\\s+table\\s+(?:if\\s+exists\\s+)?(?:only\\s+)?public\\.transition_anchor\\b"
        },
        {
          "description": "does not truncate populated data",
          "pattern": "(?:^|;)\\s*truncate(?:\\s+table)?\\b"
        },
        {
          "description": "does not panic",
          "pattern": "\\b(?:panic|segmentation fault)\\b"
        }
      ]
    }
  ]
}
