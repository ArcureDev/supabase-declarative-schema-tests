# Case: 185-no-op-convergence

## Baseline state A

```sql
create table public.no_op_convergence_guard (
  id bigint generated by default as identity primary key,
  payload text not null default 'stable'
);

comment on table public.no_op_convergence_guard is
  'An identical desired declaration must produce no migration.';
```

## Desired state B

```sql
create table public.no_op_convergence_guard (
  id bigint generated by default as identity primary key,
  payload text not null default 'stable'
);

comment on table public.no_op_convergence_guard is
  'An identical desired declaration must produce no migration.';
```

## Representative data setup

```sql
insert into public.no_op_convergence_guard (payload)
values ('first row'), ('second row');
```

## CLI-generated baseline migration files

### `20260817165800_no_op_convergence_baseline.sql`

```sql
create table "public"."no_op_convergence_guard" (
  "id"      bigint generated by default as identity not null,
  "payload" text   not null default 'stable'::text,
  constraint "no_op_convergence_guard_pkey" primary key (id)
);

comment on table "public"."no_op_convergence_guard" is 'An identical desired declaration must produce no migration.';

grant maintain, references, trigger, truncate on table "public"."no_op_convergence_guard" to "anon", "authenticated";

grant delete, insert, maintain, references, select, trigger, truncate, update on table "public"."no_op_convergence_guard" to "postgres";

grant maintain, references, trigger, truncate on table "public"."no_op_convergence_guard" to "service_role";
```


## No-op convergence assertion

- Raw sync result: **OK**
- Assertion: **OK**
- Identical state A and state B produced no migration.

## Generated transition migration files

_(no files generated)_

## Start local runtime

- Command: `npx supabase start --debug`
- Result: **OK**
- Duration: `0.9s`

## Clear local runtime before baseline

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **OK**
- Duration: `21.4s`


## Establish baseline with declarative sync --apply

- Command: `npx supabase db schema declarative sync --apply --name no_op_convergence_baseline --debug`
- Result: **OK**
- Duration: `40.5s`

## Insert representative data

- Command: `docker exec --interactive supabase_db_ds-shared-runtime psql --username postgres --dbname postgres --no-psqlrc --tuples-only --no-align --set ON_ERROR_STOP=1 --file -`
- Result: **OK**
- Duration: `0.1s`

## Baseline state capture

- Command: `docker exec --interactive supabase_db_ds-shared-runtime psql --username postgres --dbname postgres --no-psqlrc --tuples-only --no-align --set ON_ERROR_STOP=1 --file -`
- Result: **OK**
- Duration: `0.1s`


## Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **OK**
- Duration: `40.9s`
<!-- declarative-schema-command-result case="185-no-op-convergence" engine="next" command="sync" status="OK" -->

## Verify unchanged state after no-op planning

- Command: `docker exec --interactive supabase_db_ds-shared-runtime psql --username postgres --dbname postgres --no-psqlrc --tuples-only --no-align --set ON_ERROR_STOP=1 --file -`
- Result: **OK**
- Duration: `0.1s`
<!-- declarative-schema-command-result case="185-no-op-convergence" engine="next" command="sync-verification" status="OK" -->

