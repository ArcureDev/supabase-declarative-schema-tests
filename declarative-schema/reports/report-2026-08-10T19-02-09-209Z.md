# Supabase declarative schema CLI report

- Generated: 2026-08-10T19:06:30.181Z
- Supabase CLI version: `0.0.0-pr.6102`
- Checksum: `f9bd289`
- Primary engine: pg-delta next (`SUPABASE_USE_PG_DELTA_NEXT=true`)
- Snapshot fallback: failed declarative commands are retried with legacy (`SUPABASE_USE_PG_DELTA_NEXT=false`)
- Cases: 1
- Commands OK: 9
- Commands with warnings: 0
- Commands failed: 0
- Commands skipped: 0
- Runtime: one shared local PostgreSQL container, reset between projects
- Working copies: `.tmp\run-w3bj7H`

<a id="case-results"></a>

## Case results

| Case | Primary | Legacy |
| --- | --- | --- |
| `184-dependency-ordering` | **OK** | **NOT RUN** |

<!-- declarative-schema-case-result name="184-dependency-ordering" status="OK" -->

## Case: 184-dependency-ordering

### Baseline state A

```sql
create table public.dependency_source (
  id bigint generated by default as identity primary key,
  raw_value bigint not null
);
```

### Desired state B

```sql
create table public.dependency_source (
  id bigint generated by default as identity primary key,
  raw_value bigint not null
);

create function public.dependency_scale(input_value bigint)
returns bigint
language sql
immutable
strict
parallel safe
as $$
  select input_value * 2
$$;

create view public.dependency_base as
select
  source_row.id,
  public.dependency_scale(source_row.raw_value) as scaled_value
from public.dependency_source as source_row;

create view public.dependency_left as
select
  base_row.id,
  base_row.scaled_value + 1 as branch_value
from public.dependency_base as base_row;

create view public.dependency_right as
select
  base_row.id,
  base_row.scaled_value - 1 as branch_value
from public.dependency_base as base_row;

create view public.dependency_leaf as
select
  left_row.id,
  left_row.branch_value as left_value,
  right_row.branch_value as right_value,
  left_row.branch_value + right_row.branch_value as combined_value
from public.dependency_left as left_row
join public.dependency_right as right_row using (id);
```

### Representative data setup

```sql
insert into public.dependency_source (raw_value)
values
  (10),
  (-3);
```

### CLI-generated baseline migration files

#### `20260810190419_dependency_ordering_baseline.sql`

```sql
create table "public"."dependency_source" (
  "id"        bigint generated by default as identity not null,
  "raw_value" bigint not null,
  constraint "dependency_source_pkey" primary key (id)
);

grant maintain, references, trigger, truncate on table "public"."dependency_source" to "anon", "authenticated";

grant delete, insert, maintain, references, select, trigger, truncate, update on table "public"."dependency_source" to "postgres";

grant maintain, references, trigger, truncate on table "public"."dependency_source" to "service_role";
```


### Dependency-ordering migration assertion

- Raw sync result: **OK**
- Assertion: **OK**
- The migration creates the function, dependency chain, and dependency diamond in topological order without replacing the populated source table.

### Generated transition migration files

#### `20260810190517_declarative_sync.sql`

```sql
set local check_function_bodies = off;

create or replace function public.dependency_scale (
  input_value bigint
)
  returns bigint
  language sql
  immutable
  parallel safe
  strict
  AS $function$
  select input_value * 2
$function$;

create view "public"."dependency_base" AS  SELECT id,
    public.dependency_scale(raw_value) AS scaled_value
   FROM public.dependency_source source_row;

create view "public"."dependency_left" AS  SELECT id,
    (scaled_value + 1) AS branch_value
   FROM public.dependency_base base_row;

create view "public"."dependency_right" AS  SELECT id,
    (scaled_value - 1) AS branch_value
   FROM public.dependency_base base_row;

create view "public"."dependency_leaf" AS  SELECT left_row.id,
    left_row.branch_value AS left_value,
    right_row.branch_value AS right_value,
    (left_row.branch_value + right_row.branch_value) AS combined_value
   FROM (public.dependency_left left_row
     JOIN public.dependency_right right_row USING (id));

grant execute on function "public"."dependency_scale"(bigint) to public, "postgres";

grant maintain, references, trigger, truncate on table "public"."dependency_base" to "anon", "authenticated";

grant delete, insert, maintain, references, select, trigger, truncate, update on table "public"."dependency_base" to "postgres";

grant maintain, references, trigger, truncate on table "public"."dependency_base" to "service_role";

grant maintain, references, trigger, truncate on table "public"."dependency_leaf" to "anon", "authenticated";

grant delete, insert, maintain, references, select, trigger, truncate, update on table "public"."dependency_leaf" to "postgres";

grant maintain, references, trigger, truncate on table "public"."dependency_leaf" to "service_role";

grant maintain, references, trigger, truncate on table "public"."dependency_left" to "anon", "authenticated";

grant delete, insert, maintain, references, select, trigger, truncate, update on table "public"."dependency_left" to "postgres";

grant maintain, references, trigger, truncate on table "public"."dependency_left" to "service_role";

grant maintain, references, trigger, truncate on table "public"."dependency_right" to "anon", "authenticated";

grant delete, insert, maintain, references, select, trigger, truncate, update on table "public"."dependency_right" to "postgres";

grant maintain, references, trigger, truncate on table "public"."dependency_right" to "service_role";
```


### Start local runtime

- Command: `npx supabase start --debug`
- Result: **OK**

### Clear local runtime before baseline

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **OK**


### Establish baseline with declarative sync --apply

- Command: `npx supabase db schema declarative sync --apply --name dependency_ordering_baseline --debug`
- Result: **OK**

### Insert representative data

- Command: `docker exec --interactive supabase_db_ds-shared-runtime psql --username postgres --dbname postgres --no-psqlrc --tuples-only --no-align --set ON_ERROR_STOP=1 --file -`
- Result: **OK**

### Baseline state capture

- Command: `docker exec --interactive supabase_db_ds-shared-runtime psql --username postgres --dbname postgres --no-psqlrc --tuples-only --no-align --set ON_ERROR_STOP=1 --file -`
- Result: **OK**


### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **OK**
<!-- declarative-schema-command-result case="184-dependency-ordering" engine="next" command="sync" status="OK" -->

### Apply generated transition migration

- Command: `npx supabase migration up --local --debug`
- Result: **OK**

### Verify desired state B

- Command: `docker exec --interactive supabase_db_ds-shared-runtime psql --username postgres --dbname postgres --no-psqlrc --tuples-only --no-align --set ON_ERROR_STOP=1 --file -`
- Result: **OK**

### Sync verification / convergence (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **OK**
<!-- declarative-schema-command-result case="184-dependency-ordering" engine="next" command="sync-verification" status="OK" -->

