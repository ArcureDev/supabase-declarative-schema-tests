# Supabase declarative schema CLI report

- Generated: 2026-08-08T18:17:03.030Z
- Primary engine: pg-delta next (`SUPABASE_USE_PG_DELTA_NEXT=true`)
- Fallback: failed declarative commands are retried with legacy (`SUPABASE_USE_PG_DELTA_NEXT=false`)
- Migration cases: 29
- Commands OK: 0
- Commands with warnings: 0
- Commands failed: 59
- Commands skipped: 57
- Runtime: one shared local PostgreSQL container, reset between projects
- Working copies: `.tmp\run-JsZDFQ`

## Case results

- `100-multirange-type`: **FAILED**
<!-- declarative-schema-case-result name="100-multirange-type" status="FAILED" -->
- `21-generated-stored-column`: **FAILED**
<!-- declarative-schema-case-result name="21-generated-stored-column" status="FAILED" -->
- `22-identity-column-options`: **FAILED**
<!-- declarative-schema-case-result name="22-identity-column-options" status="FAILED" -->
- `23-unlogged-table`: **FAILED**
<!-- declarative-schema-case-result name="23-unlogged-table" status="FAILED" -->
- `24-typed-table`: **FAILED**
<!-- declarative-schema-case-result name="24-typed-table" status="FAILED" -->
- `25-composite-primary-key`: **FAILED**
<!-- declarative-schema-case-result name="25-composite-primary-key" status="FAILED" -->
- `26-unique-constraint`: **FAILED**
<!-- declarative-schema-case-result name="26-unique-constraint" status="FAILED" -->
- `27-unique-nulls-not-distinct`: **FAILED**
<!-- declarative-schema-case-result name="27-unique-nulls-not-distinct" status="FAILED" -->
- `28-exclusion-constraint`: **FAILED**
<!-- declarative-schema-case-result name="28-exclusion-constraint" status="FAILED" -->
- `29-deferrable-unique-constraint`: **FAILED**
<!-- declarative-schema-case-result name="29-deferrable-unique-constraint" status="FAILED" -->
- `30-deferrable-foreign-key`: **FAILED**
<!-- declarative-schema-case-result name="30-deferrable-foreign-key" status="FAILED" -->
- `31-self-referencing-foreign-key`: **FAILED**
<!-- declarative-schema-case-result name="31-self-referencing-foreign-key" status="FAILED" -->
- `32-multi-column-foreign-key`: **FAILED**
<!-- declarative-schema-case-result name="32-multi-column-foreign-key" status="FAILED" -->
- `33-column-default-expression`: **FAILED**
<!-- declarative-schema-case-result name="33-column-default-expression" status="FAILED" -->
- `34-column-collation`: **FAILED**
<!-- declarative-schema-case-result name="34-column-collation" status="FAILED" -->
- `35-column-compression-setting`: **FAILED**
<!-- declarative-schema-case-result name="35-column-compression-setting" status="FAILED" -->
- `36-table-storage-parameters`: **FAILED**
<!-- declarative-schema-case-result name="36-table-storage-parameters" status="FAILED" -->
- `37-table-tablespace`: **FAILED**
<!-- declarative-schema-case-result name="37-table-tablespace" status="FAILED" -->
- `38-table-inheritance`: **FAILED**
<!-- declarative-schema-case-result name="38-table-inheritance" status="FAILED" -->
- `39-range-partitioned-table`: **FAILED**
<!-- declarative-schema-case-result name="39-range-partitioned-table" status="FAILED" -->
- `40-list-partitioned-table`: **FAILED**
<!-- declarative-schema-case-result name="40-list-partitioned-table" status="FAILED" -->
- `41-hash-partitioned-table`: **FAILED**
<!-- declarative-schema-case-result name="41-hash-partitioned-table" status="FAILED" -->
- `42-default-partition`: **FAILED**
<!-- declarative-schema-case-result name="42-default-partition" status="FAILED" -->
- `43-multi-level-partitioning`: **FAILED**
<!-- declarative-schema-case-result name="43-multi-level-partitioning" status="FAILED" -->
- `44-partition-constraint-and-bound`: **FAILED**
<!-- declarative-schema-case-result name="44-partition-constraint-and-bound" status="FAILED" -->
- `45-table-replica-identity`: **FAILED**
<!-- declarative-schema-case-result name="45-table-replica-identity" status="FAILED" -->
- `46-table-and-column-comments`: **FAILED**
<!-- declarative-schema-case-result name="46-table-and-column-comments" status="FAILED" -->
- `47-alter-table-add-rename-and-drop-column`: **FAILED**
<!-- declarative-schema-case-result name="47-alter-table-add-rename-and-drop-column" status="FAILED" -->
- `48-alter-table-change-column-type-with-using`: **FAILED**
<!-- declarative-schema-case-result name="48-alter-table-change-column-type-with-using" status="FAILED" -->

## 100-multirange-type

### Fixture migration SQL

```sql
create type public.fixture_int_range as range (
  subtype = integer
);

create table public.fixture_int_multirange_values (
  id bigint generated always as identity primary key,
  spans public.fixture_int_rangemultirange not null
);
```

### Generated declarative files (pg-delta next)

_(no files generated)_

### Generated declarative files (legacy)

_(no files generated)_

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --reset --overwrite --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
Starting database...
Initialising schema...
Seeding globals from roles.sql...
Applying migration 20260101000000_case.sql...
Stopping containers...
ERROR: type "public.fixture_int_rangemultirange" does not exist (SQLSTATE 42704)
At statement: 1                                                                 
create table public.fixture_int_multirange_values (                             
  id bigint generated always as identity primary key,                           
  spans public.fixture_int_rangemultirange not null                             
        ^                                                                       
Try rerunning the command with --debug to troubleshoot the error.
failed to start local database: exit 1
```

### Generate fallback (legacy)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta legacy implementation.
Starting database...
Initialising schema...
Seeding globals from roles.sql...
Applying migration 20260101000000_case.sql...
Stopping containers...
ERROR: type "public.fixture_int_rangemultirange" does not exist (SQLSTATE 42704)
At statement: 1                                                                 
create table public.fixture_int_multirange_values (                             
  id bigint generated always as identity primary key,                           
  spans public.fixture_int_rangemultirange not null                             
        ^                                                                       
Try rerunning the command with --debug to troubleshoot the error.
failed to start local database: exit 1
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 21-generated-stored-column

### Fixture migration SQL

```sql
create table public.invoice_lines (
  id bigint generated always as identity primary key,
  quantity integer not null,
  unit_price numeric(12, 2) not null,
  line_total numeric(14, 2)
    generated always as (quantity * unit_price) stored
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 22-identity-column-options

### Fixture migration SQL

```sql
create table public.numbered_documents (
  id bigint generated by default as identity (
    start with 1000
    increment by 5
    minvalue 1000
    maxvalue 999999
    cache 20
    cycle
  ) primary key,
  title text not null
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 23-unlogged-table

### Fixture migration SQL

```sql
create unlogged table public.transient_imports (
  id bigint generated always as identity primary key,
  source_name text not null,
  payload jsonb not null,
  imported_at timestamptz not null default now()
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 24-typed-table

### Fixture migration SQL

```sql
create type public.measurement_row as (
  observed_at timestamptz,
  measured_value double precision
);

create table public.measurements of public.measurement_row;
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 25-composite-primary-key

### Fixture migration SQL

```sql
create table public.project_memberships (
  project_id bigint not null,
  member_id bigint not null,
  joined_at timestamptz not null default now(),
  constraint project_memberships_pkey primary key (project_id, member_id)
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 26-unique-constraint

### Fixture migration SQL

```sql
create table public.usernames (
  id bigint generated always as identity primary key,
  username text not null,
  constraint usernames_username_key unique (username)
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 27-unique-nulls-not-distinct

### Fixture migration SQL

```sql
create table public.external_accounts (
  id bigint generated always as identity primary key,
  external_id text,
  constraint external_accounts_external_id_key
    unique nulls not distinct (external_id)
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 28-exclusion-constraint

### Fixture migration SQL

```sql
create table public.reservations (
  id bigint generated always as identity primary key,
  reserved_during tstzrange not null,
  constraint reservations_no_overlap
    exclude using gist (reserved_during with &&)
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 29-deferrable-unique-constraint

### Fixture migration SQL

```sql
create table public.imported_records (
  id bigint generated always as identity primary key,
  external_id text not null,
  constraint imported_records_external_id_key
    unique (external_id)
    deferrable initially deferred
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 30-deferrable-foreign-key

### Fixture migration SQL

```sql
create table public.organizations (
  id bigint generated always as identity primary key,
  name text not null
);

create table public.organization_members (
  id bigint generated always as identity primary key,
  organization_id bigint not null,
  display_name text not null,
  constraint organization_members_organization_id_fkey
    foreign key (organization_id)
    references public.organizations (id)
    deferrable initially deferred
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 31-self-referencing-foreign-key

### Fixture migration SQL

```sql
create table public.categories (
  id bigint generated always as identity primary key,
  parent_category_id bigint,
  name text not null,
  constraint categories_parent_category_id_fkey
    foreign key (parent_category_id)
    references public.categories (id)
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 32-multi-column-foreign-key

### Fixture migration SQL

```sql
create table public.regions (
  country_code text not null,
  region_code text not null,
  name text not null,
  constraint regions_pkey primary key (country_code, region_code)
);

create table public.warehouses (
  id bigint generated always as identity primary key,
  country_code text not null,
  region_code text not null,
  constraint warehouses_region_fkey
    foreign key (country_code, region_code)
    references public.regions (country_code, region_code)
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 33-column-default-expression

### Fixture migration SQL

```sql
create table public.delivery_windows (
  id bigint generated always as identity primary key,
  opens_at timestamptz not null default (now() + interval '1 day')
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 34-column-collation

### Fixture migration SQL

```sql
create table public.catalog_labels (
  id bigint generated always as identity primary key,
  label text collate "C" not null
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 35-column-compression-setting

### Fixture migration SQL

```sql
create table public.archived_documents (
  id bigint generated always as identity primary key,
  body text compression pglz not null
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 36-table-storage-parameters

### Fixture migration SQL

```sql
create table public.event_buffer (
  id bigint generated always as identity primary key,
  payload jsonb not null
) with (
  fillfactor = 70,
  autovacuum_vacuum_scale_factor = 0.05
);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 37-table-tablespace

### Fixture migration SQL

```sql
create table public.tablespace_documents (
  id bigint generated always as identity primary key,
  title text not null
) tablespace pg_default;
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 38-table-inheritance

### Fixture migration SQL

```sql
create table public.content_items (
  id bigint not null,
  title text not null,
  published_at timestamptz
);

create table public.articles (
  body text not null
) inherits (public.content_items);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 39-range-partitioned-table

### Fixture migration SQL

```sql
create table public.time_entries (
  id bigint not null,
  recorded_on date not null,
  minutes integer not null,
  constraint time_entries_pkey primary key (id, recorded_on)
) partition by range (recorded_on);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 40-list-partitioned-table

### Fixture migration SQL

```sql
create table public.regional_announcements (
  id bigint not null,
  region_code text not null,
  message text not null,
  constraint regional_announcements_pkey primary key (id, region_code)
) partition by list (region_code);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 41-hash-partitioned-table

### Fixture migration SQL

```sql
create table public.hash_shards (
  id bigint not null,
  payload text not null,
  constraint hash_shards_pkey primary key (id)
) partition by hash (id);
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 42-default-partition

### Fixture migration SQL

```sql
create table public.sensor_readings (
  id bigint not null,
  recorded_on date not null,
  value numeric not null,
  constraint sensor_readings_pkey primary key (id, recorded_on)
) partition by range (recorded_on);

create table public.sensor_readings_default
partition of public.sensor_readings
default;
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 43-multi-level-partitioning

### Fixture migration SQL

```sql
create table public.metric_samples (
  id bigint not null,
  recorded_on date not null,
  region_code text not null,
  value numeric not null,
  constraint metric_samples_pkey primary key (id, recorded_on, region_code)
) partition by range (recorded_on);

create table public.metric_samples_2024
partition of public.metric_samples
for values from ('2024-01-01') to ('2025-01-01')
partition by list (region_code);

create table public.metric_samples_2024_eu
partition of public.metric_samples_2024
for values in ('eu');
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 44-partition-constraint-and-bound

### Fixture migration SQL

```sql
create table public.invoice_archive (
  id bigint not null,
  issued_on date not null,
  amount numeric(12, 2) not null,
  constraint invoice_archive_pkey primary key (id, issued_on)
) partition by range (issued_on);

create table public.invoice_archive_2023
partition of public.invoice_archive
for values from ('2023-01-01') to ('2024-01-01');
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 45-table-replica-identity

### Fixture migration SQL

```sql
create table public.replica_widgets (
  id bigint generated always as identity primary key,
  label text not null
);

alter table public.replica_widgets replica identity full;
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 46-table-and-column-comments

### Fixture migration SQL

```sql
create table public.documented_items (
  id bigint generated always as identity primary key,
  title text not null
);

comment on table public.documented_items is 'Items with documented metadata.';
comment on column public.documented_items.title is 'Human-readable item title.';
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 47-alter-table-add-rename-and-drop-column

### Fixture migration SQL

```sql
create table public.mutable_profiles (
  id bigint generated always as identity primary key,
  nickname text,
  legacy_code text
);

alter table public.mutable_profiles
  add column display_name text,
  drop column legacy_code;

alter table public.mutable_profiles
  rename column nickname to handle;
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

## 48-alter-table-change-column-type-with-using

### Fixture migration SQL

```sql
create table public.measurement_logs (
  id bigint generated always as identity primary key,
  reading text not null
);

alter table public.measurement_logs
  alter column reading type integer
  using reading::integer;
```

### Reset

- Command: `npx supabase db reset --local --no-seed --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
supabase start is not running.
```

### Generate (pg-delta next)

- Command: `npx supabase db schema declarative generate --local --overwrite --debug`
- Result: **SKIPPED**

```text
Database reset failed, so declarative generation was skipped.
```

### Sync (pg-delta next)

- Command: `npx supabase db schema declarative sync --debug`
- Result: **SKIPPED**

```text
Generate failed, so declarative sync was skipped.
```

### Sync verification (pg-delta next)

- Command: `npx supabase db schema declarative sync --no-apply --debug`
- Result: **ERROR**
- Exit code: `1`

```text
NotFound: FileSystem.readFile (C:\Users\Arcure\.supabase\profile)
Using pg-delta next implementation.
no declarative schema found. Run supabase db schema declarative generate first
```

